torch>=2.8
torchvision>=0.23
transformers>=5.2
timm==1.0.9
accelerate>=1.6
bitsandbytes>=0.46.0
gradio
einops
ninja
scikit-image
eva-decord
scikit-learn
matplotlib
# The Qwen3.5 backbone needs transformers >= 5.2. If you want to run the
# original Qwen2.5-VL checkpoints instead, use the old pins in a separate
# environment: torch==2.6.0 torchvision==0.21.0 transformers==4.50.1 accelerate==1.4.0
